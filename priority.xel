entry main() {
    let array = [10, 20, 30]
    array[0] = 12
    let b = 24
    array[1] = b * array[0]
    while array[0] > 0 {
        array.push(0)
        println(array.len() + "")
        array[0] -= 1
    }
    
}