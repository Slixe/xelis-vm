entry main() {
    let a = [1+1, 2+2]
    a[0] = 1

    let b = test(a[1] * a[0] + 10)
    
    return fibonacci(10)
}

func test(a: number): number {
    return a * 15
}

func fibonacci(n: number): number {
    if n <= 1 {
        return n
    }

    return fibonacci(n - 1) + fibonacci(n - 2)
}